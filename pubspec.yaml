name: camera_test_web
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.9.5
  hive: ^2.1.0
  hive_flutter: ^1.1.0
  uuid: ^3.0.6



flutter:
  uses-material-design: true